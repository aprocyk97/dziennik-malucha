import React, { FC } from 'react'
import styled from 'styled-components';

interface IIcon{

}


export const Icon: FC<IIcon> = (props) => {
    return (
        <div>
            
        </div>
    )
}
